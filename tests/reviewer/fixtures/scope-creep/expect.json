{
  "issue": 102,
  "findings": [
    {
      "category": "Scope",
      "file": "config/mail.php",
      "anchor": "'default' => env('MAIL_MAILER', 'log')",
      "note": "mailer default flipped to log — unrelated to the status filter, and silently disables mail anywhere MAIL_MAILER is unset"
    }
  ]
}
